## REMOVED Requirements

### Requirement: Log format
**Reason**: Moved to `access-log` capability. Log entry formatting is now the responsibility of the `accesslog` package.
**Migration**: Requirements are identical in `access-log` spec. No behavioral change.

### Requirement: Log deduplication
**Reason**: Moved to `access-log` capability. Deduplication logic is now in the `accesslog` package.
**Migration**: Requirements are identical in `access-log` spec. No behavioral change.

### Requirement: Infrastructure path filtering
**Reason**: Moved to `access-log` capability. Infrastructure path filtering is now in the `accesslog` package.
**Migration**: Requirements are identical in `access-log` spec. No behavioral change.

